# Dev profile overrides
# Disable abuse/rate-limit checks for local development
app.security.rate-limit.enabled=false

# Enable OTP send rate-limit bypass only for local development profile
app.security.rate-limit.send-otp.dev-bypass-enabled=true
